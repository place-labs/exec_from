name: exec_from
version: 1.0.1
license: MIT
authors:
  - Stephen von Takach <steve@acaprojects.com>

targets:
  exec_from:
    main: src/exec_from.cr

scripts:
  postinstall: mkdir -p ../../bin && crystal build --release --no-debug -o ../../bin/exec_from ./src/exec_from.cr

executables:
  - exec_from
