name: exec_from
version: 1.0.0
license: MIT
authors:
  - Stephen von Takach <steve@acaprojects.com>

targets:
  exec_from:
    main: src/exec_from.cr

scripts:
  postinstall: crystal build --release --no-debug -o ./bin/exec_from ./src/exec_from.cr

executables:
  - exec_from
